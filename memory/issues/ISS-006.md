---
id: ISS-006
title: "scheduler quiet ~36.5h — heartbeat missed all three 2026-09-16 slots (fresh recurrence of ISS-005 missed-dispatch pattern, crossed 36h)"
status: open
severity: high
category: unknown
detected_by: heartbeat
detected_at: 2026-09-17
resolved_at:
affected_skills: [heartbeat]
root_cause: "Unconfirmed — same signature as ISS-005. memory/cron-state.json shows heartbeat last_success 2026-09-15T21:29:24Z with last_status 'success' (not 'dispatched'), so this is missed *dispatch*, not a hung run: the scheduled workflow did not fire heartbeat for the 09-16 08:00/14:00/20:00 slots or the 09-17 08:00 slot. No weekly cohort skill (atlas Sun 04:00 / atlas-layers Sun 05:00 / atlas-improve monthly) was due inside the window, so this run cannot confirm whether the miss is heartbeat-cadence-specific or scheduler-wide. This 2026-09-17 10:00 UTC run is the 09-17 08:00 slot dispatching ~2h late (catch-up)."
fix_pr:
---

# ISS-006 — scheduler quiet window (~36.5h), heartbeat missed all three 09-16 slots

## Symptom

As of this 2026-09-17 10:00 UTC heartbeat run, `memory/cron-state.json` shows heartbeat frozen at:
- `last_success: 2026-09-15T21:29:24Z`, `last_dispatch: 2026-09-15T21:26:44Z`, `last_status: success`, `consecutive_failures: 0`, `success_rate: 0.89` (247/276).
- Self-check: last_success is **~36.5h ago** (09-15 21:29 → 09-17 10:00), past the 36h P0 self-check threshold → **flagged → 🔴 DEGRADED**.

Missed slots in the window:
- **09-16 08:00 — MISSED**
- **09-16 14:00 — MISSED**
- **09-16 20:00 — MISSED**
- **09-17 08:00 — dispatched ~2h late (this run, 10:00)** — scheduler alive again.

The entire 2026-09-16 day (three heartbeat slots) produced no run, no state advance, and no log.

## Impact

- **~36.5h with no fleet health checks** between the clean 09-15 20:00 run (last_success 21:29Z, ~1.4h late) and this catch-up run.
- `docs/status.md` went stale (Updated 2026-09-15 21:27 UTC) for ~1.5 days, past the ~8h staleness banner.
- No weekly/monthly atlas-cohort skill was scheduled inside the window (next atlas/atlas-layers Sunday 2026-09-20; atlas-improve 2026-10-01), so no atlas output was lost this cycle.

## Distinction / lineage

Same signature as **ISS-005** (resolved 2026-09-05): `last_status: success` with `last_dispatch` frozen — a **missed dispatch**, not a hung run (contrast ISS-004, which was `dispatched` and never reported back). ISS-005 established this as a **standing** GitHub Actions `schedule:` reliability problem (cron delivery is best-effort and drops under load / after inactivity). Heartbeat ran acceptably 09-05 → 09-15 (occasional lateness but slots landed), then this fresh ~36.5h quiet window opened. Filed as a **new** issue (not a reopen of ISS-005) per ISS-005's own closure note that a fresh miss after resolution is a fresh signal. The 09-14/09-15 heartbeat checkpoints pre-committed to filing once the self-check crossed 36h — that threshold has now been crossed.

## Suggested next step / checkpoint

- **Recovery criterion:** 3 consecutive clean heartbeat slots (state advances + log written). This run is **1 of 3** (09-17 08:00 slot). If 09-17 14:00 and 20:00 dispatch cleanly → 3 of 3 → downgrade to medium, then resolve. Reset the counter on any miss (ISS-005 broke at 2 of 3 repeatedly before holding).
- **Weekly cohort test:** watch Sunday 2026-09-20 04:00 (atlas) / 05:00 (atlas-layers). If either misses, the weekly dimension is affected too → escalate to a scheduler-wide fix rather than treating this as heartbeat-only.
- **Durable mitigation** (only real close path, since skill-repair is `enabled: false` so no code fix lands automatically): a redundant offset heartbeat cron, or an external cron-job.org → `repository_dispatch` ping. Absent that, each cycle re-confirms via heartbeat's own self-check.
