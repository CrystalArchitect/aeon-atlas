# Watched Repositories (Topic Index)

See `../watched-repos.md` for the authoritative list of monitored repos.

This file exists for backward compatibility with skills that look for the topics-scoped path. The canonical list lives at the memory root for broader accessibility across all skills.

To modify the watched repos list, edit `memory/watched-repos.md`.
